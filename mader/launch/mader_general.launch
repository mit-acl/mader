<launch>
	<arg name="quad" default="SQ01s"/>
	<arg name="gui" default="true"/>
	<arg name="rviz" default="true"/>
	<arg name="environment" default="true"/>
	<!-- Start mader commands 
	<node ns="$(arg quad)" name="mader_commands" pkg="mader" type="mader_commands.py" output="screen"/>
	 -->
	<!-- Start Behaviour Selector-->
	  <node pkg="behavior_selector" type="behavior_selector_node.py" name="behavior_selector" output="screen" />


  <!-- Don't run this, for instance, for ssh connections -->
<group if="$(arg rviz)">

	<!-- Start Rviz -->
	<node pkg="rviz" type="rviz" name="rviz" args="-d $(find mader)/rviz_cfgs/cvx_all.rviz"/>

</group>

  <!-- Don't run this, for instance, for ssh connections -->
<group if="$(arg gui)">
	<!-- Start GUI 	-->
	<node pkg="rqt_gui" type="rqt_gui" name="rqt_gui" args="--perspective-file $(find mader)/rqt_cfgs/demo_interface.perspective"/>

</group>

<group if="$(arg environment)">
    <!-- Start dynamic obstacles -->
	<node name="dynamic_obstacles" pkg="mader" type="dynamic_obstacles.py" output="screen" args="150"/>
	<!--<node name="dynamic_obstacles" pkg="mader" type="dynamic_forest.py" output="screen"/>-->
</group>s	

</launch>
